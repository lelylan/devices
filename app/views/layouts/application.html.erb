<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8"> 
  <title>Lelylan Devices</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag "jquery.tagsinput.js", "highcharts.js" %>
  <%= csrf_meta_tag %>
</head>

<body">
  <div class="container">
    <div class="header">
      <h1>Lelylan Devices</h1>
      <p>
      <b>Lelylan Devices let you open the communication between the physical world and the web through
        easily enabling the definition,the monitoring and control of everything you want. 
      </p>
    </div>
    <div class="navigation">
        <% if current_user %>
          <div style="float: right">
            Logged in as <%= current_user.email %>.
            <%= link_to "Log out", logout_path %>
          </div>
          <div>
            <%= link_to("Home", current_user)  %>
          </div>
        <% else %>
          <%= link_to "Sign up", signup_path %> or
          <%= link_to "log in", login_path %>
        <% end %>
      </div>
      <br/>


    <% if flash.notice %>
      <div class="flash_notice">
        <%= flash.notice %>
      </div>
    <% end %>

    <% if flash.alert %>
      <div class=flash_alert>
        <%= flash.alert %>
        <% if @info %>
          <p>Additional information: 
          <%= @info.to_json%>
          </p>
        <% end %>
      </div>
    <% end %>

    <%= yield %>

  </div>
</body>
</html>
