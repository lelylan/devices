
<% connections.each_with_index do |connection, index| %>
    { "function_uri": "<%=connection.function_uri%>",
      "name": "<%=raw connection.name%>" }<% if (connections.length - 1 > index) %>,<%end%>
<% end %>
