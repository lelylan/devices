
<% connections.each_with_index do |connection, index| %>
    { "name": "<%=raw connection.name%>", 
      "uri": "<%=connection.uri%>", 
      "function_uri": "<%=connection.function_uri%>" }<% if (connections.length - 1 > index) %>,<%end%>
<% end %>
